{
  "name": "ascendos",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Career accelerator — static UI + optional free-tier aggregate APIs",
  "scripts": {
    "start:oracle": "node server/oracle-aggregate.mjs",
    "deploy:cf": "wrangler deploy",
    "deploy:vercel": "vercel --prod"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
